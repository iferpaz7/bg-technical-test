import { Route } from '@angular/router';
import { DashboardComponent } from '@modules/home/ui/dashboard/dashboard.component';

export const routes: Route[] = [
  {
    path: '',
    component: DashboardComponent,
  },
];
